const somar = require('./metodos/somar');
const subtrair = require('./metodos/subtrair');
const multiplicar = require('./metodos/multiplicar');
const dividir = require('./metodos/dividir');

console.log (somar (10,20));
console.log (subtrair (10,20));
console.log (multiplicar (10,20));
console.log (dividir (10,20));